{% extends 'base.html.twig' %}

{% block title %}All posts{% endblock %}

{% block body %}
    {% if posts|length > 0 %}
    {% for post in posts %}

    <div>
    <a href ="{{path('app_micro_post_show',{post:post.id})}}">
    {{post.id}}
    </a>
    </div>

    {% endfor %}
    {% endif %}
{% endblock %}
